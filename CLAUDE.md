@AGENTS.md

# 차곡 🌰 작업 규칙

> 공통 규칙은 전역 CLAUDE.md에. 여기는 **이 앱에서만** 지킬 것.
> 작업 시작 전 `🔧 운동식단가이드앱 진행상황.md`를 먼저 읽는다.

## 문서가 먼저다
- `PRD.md`(왜) → `SPEC.md`(무엇이 되면 완성) → `TASKS.md`(어떤 순서) 순으로 이미 정해져 있다.
- 기능을 고치면 **SPEC의 해당 FN 번호도 같이 고친다.** PRD에 반대되는 문장이 남아 있으면 그것도 고친다.
  (이 프로젝트는 과거 PRD 안에서 모순 12곳이 나와 크게 고생했다)
- 지시할 때는 번호로: `FN-31만 고쳐줘`, `M2까지 만들어줘`.

## 🔴 이 앱이 실패하는 유일한 방법 — "복잡한 건 안 쓰게 됨"
1. **2번 안에 끝난다** (탭 2번 안에 기록·촬영)
2. **입력창은 바로 뜬다** (중간 화면 금지)
3. **빈칸을 미리 채운다** (직전 값 상속)
4. **설정을 묻지 않는다** (첫 실행 설문·프로필 없음)
5. **글자 대신 그림**
6. **강조는 하나만** — 노란 강조는 화면당 1개

## 말투 (2026-08-11 확정)
- 🌶️ **식단은 매운맛.** 과자류는 무조건 배드 3종 + 정크 많음. 봐주지 않는다
- 🍀 **운동은 부드럽게.** 기록이 낮아져도 아무 말 안 함, 연속일수(스트릭) 안 만듦
- 🔒 **어느 쪽이든 몸무게·체형·외모는 한 글자도 쓰지 않는다.** 음식·기록 이야기만

## 코드 쓸 때 꼭 확인할 것

### CSS 클래스 이름은 만들기 전에 검색한다
`.rt`·`.empty` 가 겹쳐서 엉뚱한 여백과 네모가 생긴 적이 **두 번** 있다.
**빌드는 통과하고 눈으로만 잡힌다.** 새 클래스는 `globals.css`에서 먼저 찾아본다.

### 화면을 만들었으면 반드시 눈으로 본다
`npm run build` 통과 ≠ 화면 정상. 특히 이 앱에서 나온 사고들:
- 세로 flex 안에서 영상칸이 눌리거나 사라짐 → `.player`에 `flex:none` + `min-height` 있음
- **세로(Shorts)와 가로 영상은 다른 경로.** 가로만 보면 절반만 본 것

### 폰에서 확인한다
컴퓨터 화면으로는 안 보이는 문제가 계속 나왔다 (흰 화면·앨범 버튼·눌린 영상).
배포본 https://chagok-woowooff1.vercel.app 로 폰에서 직접 눌러본다.

### 사진·저장
- 글자는 `localStorage`, **사진은 IndexedDB**. 사진을 localStorage에 넣지 않는다 (5MB 한계)
- 저장은 `saveState()` **한 곳에서만**
- 🔒 **눈바디 사진은 어떤 서버로도 보내지 않는다.** `app/body/`·`components/BodyCamera.tsx`에 fetch가 없어야 한다

### AI 지시문을 고칠 때
- **판정 기준과 말투 규칙을 반드시 분리**한다. 섞으면 말투가 판정을 물렁하게 만든다
- 예시 문장을 주면 **그대로 베낀다.** "베끼지 말고 사진에 맞게 새로 쓰라"를 같이 적는다
- 모델은 추측하지 말고 **목록 조회 + 실제 시험** 후 고른다

## 배포
- 앱: https://chagok-woowooff1.vercel.app (Vercel `woowooff1/chagok`)
- ⚠️ `chagok.vercel.app` 은 **남의 앱**이다. 헷갈리지 말 것
- 깃허브(공개): https://github.com/woowooff/chagok
  - 이 폴더는 `jeju_vibe` 저장소의 일부다. 차곡만 올릴 때:
    `git subtree push --prefix=fitness-guide chagok main`
- 환경변수 `GEMINI_API_KEY`는 **로컬 `.env.local` 과 Vercel 양쪽에** 있어야 한다
- ⚠️ `hevy_workout_data.csv`(개인 운동기록)와 `.env.local`은 **깃에 올리지 않는다**
