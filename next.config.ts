import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // 폰에서 열려면 이게 있어야 한다 (2026-08-11).
  //
  // Next.js는 개발 중일 때 localhost 말고 다른 주소로 들어오는 요청을 기본으로 막는다.
  // 그래서 폰에서 http://192.168.x.x:3000 으로 들어가면 화면이 하얗게만 뜬다
  // (페이지는 오는데 자바스크립트 파일이 차단됨).
  //
  // ⚠️ 방화벽 문제가 아니다. 여기에 집 와이파이 주소를 적어주면 된다.
  //    공유기가 IP를 바꿔도 되게 대역 전체를 열어둔다.
  //    다른 와이파이(회사 등)에서 쓰려면 그 대역을 한 줄 더 넣고 서버를 껐다 켠다.
  allowedDevOrigins: [
    "192.168.11.17",
    "192.168.11.*",
    "192.168.0.*",
    "192.168.1.*",
    "172.30.1.*", // KT 공유기 기본 대역
  ],
};

export default nextConfig;
