# PRD — 차곡 🌰

> **앱 이름: 차곡** (2026-08-10 확정) — 차곡차곡 쌓다
> **캐릭터: 다람쥐 + 도토리** 🐿️🌰

## 🗑️ 버린 이름 — 삐약짐 (같은 날 폐기)

처음엔 「삐약짐」(병아리+gym)으로 정했다. *"운동 초보 = 헬린이 = 병아리"* 라는 논리였다.
그런데 **그 논리 자체가 함정**이었다.

> 우경님: *"뭔가 삐약짐은 계속 쓰기 싫을거 같애 만년 초보자 같아서"*

**잘하게 될수록 이름이 안 맞아진다.** 1년 뒤에도 앱이 나를 병아리라고 부르면 열기 싫어진다.

## ✅ 왜 「차곡」인가 — 오래 쓸수록 맞아지는 이름

이 앱의 정체는 **쌓아서 나중에 보는 것**이다.
- 눈바디 → `31일 차이!`
- 기록 탭 → 달력에 점이 차곡차곡
- 루틴 → `지난번 40kg` 이 쌓여서 다음이 보임

| | 1년 뒤 |
|---|---|
| 삐약짐 | *"나 아직도 병아리야?"* → 이름이 **안 맞아짐** |
| **차곡** | *"1년치가 쌓였네"* → 이름이 **더 맞아짐** |

- **초보/고수 개념이 없다.** 잘하든 못하든 계속 맞는다
- **다람쥐**는 볼주머니에 도토리를 차곡차곡 채우는 동물 — 기록을 채우는 그림과 정확히 겹친다
- 🔑 **루틴 하나 끝내면 도토리 하나 🌰** — 이름·캐릭터·기능이 한 줄로 이어진다

## 브랜드 방향
(우경님 지시: *"상큼 발랄 귀여운 / 예쁘고 심각하지 않게"* / *"UX 무조건 쉽고 직관적"*)

| | |
|---|---|
| 색 | 도토리 주황 `#E58A3C` + 크림 `#FFF7EC` + 잎 초록 `#86C06B` |
| 글씨·테두리 | 도토리 갈색 `#3D2B1C` (검정 아님 — 딱딱해 보이지 않게) |
| 캐릭터 | 볼이 빵빵한 **다람쥐** 🐿️, 도토리를 들고 있다 |
| 말투 | *"오늘도 차곡차곡"* / *"차곡차곡 잘하고 있어요!"* |
| 식단 판정 | 굿·낫뱃·배드 세 단계. **혼내지 않는다** (배드도 빨강 아닌 살구색) |
| 질감 | 굵은 테두리 + 도톰한 그림자 = **장난감처럼, 심각하지 않게**. 헬스장에서 땀 흘리며 눌러도 잘 눌리게 |

- ⚠️ Hevy는 운동 잘하는 사람용. **우리는 「기구는 쓸 줄 알지만 좋은 루틴 영상을 저장해두고 참고하는 사람」용**

- 작성일: 2026-08-10
- 작성자: 우경님 인터뷰 + 클로드 정리
- 근거 자료: `01_벤치마킹조사_PeakBFF.md`, `02_후기조사_Hevy와경쟁앱.md`
- 형식: 기획 규칙 「육하원칙 + 1페이지 5항목」

---

## 📐 육하원칙

| 항목 | 내용 |
|---|---|
| **Who** 주인공 | **우경님 본인.** 헬스장에서 근력운동을 하고, **Hevy를 실제로 쓰고 있는 사람.** 코딩은 초보<br>(2차: 링크를 받아 써볼 지인) |
| **When/Where** 언제·어디서 | **헬스장에서, 운동 직전·직후에 폰으로.** 그리고 **밥 먹을 때 하루 2~3회** |
| **What** 무엇을 | ① 오늘 할 운동을 고르고 → ② **내가 직접 고른 가이드 영상의 필요한 구간만** 보고 → ③ 세트·횟수·무게를 기록하고 → ④ 밥 사진을 찍으면 **굿/낫뱃/배드**로 알려주고 → ⑤ **눈바디를 앞·옆·뒤로 찍어두면 한 달 뒤 나란히 비교**해주는 웹앱 |
| **Why** 왜 | Hevy는 세 군데서 막힌다.<br>**①** 운동마다 시연 영상이 있지만 **내가 따라 하고 싶은 영상으로 바꿀 수 없다.** 그래서 유튜브·릴스를 따로 열어 원하는 부분을 매번 찾아 감아야 한다.<br>**②** 식단 기능이 **아예 없다.** 그렇다고 칼로리를 일일이 세기는 귀찮다.<br>**③** 눈바디 사진을 **찍어도 앨범에 흩어져서** 한 달 전과 비교하려면 스크롤을 한참 올려야 한다. |
| **How** 어떻게 | 운동 선택 → 가이드 영상 → 기록 → 식단 사진 → 눈바디 (아래 유저 흐름) |

---

# 1페이지 PRD

## #1 제품 개요 (Why & Who)

**한 문장 문제 정의**
> 헬스장에서 운동하는 나는, **따라 하고 싶은 유튜브·릴스 영상이 있어도 그걸 운동 기록 앱에 붙여둘 수 없어서** 매번 영상을 다시 찾아 필요한 부분까지 감아야 하고, **식단은 아예 관리를 못 하고 있다.**
> 나는 **「운동마다 내가 고른 영상 구간을 붙여두기」와 「사진 한 장으로 끝나는 식단 판정」** 으로, **운동과 식단을 한 앱에서 3초 안에 끝내는 변화**를 만든다.

**왜 이걸 만드나 — 조사로 확인된 근거**

1. **내 불편은 나만의 것이 아니다.** Hevy 혹평 80건 중 두 건이 정확히 같은 말을 한다.
   - *"짜여진 프로그램을 넣으려면 운동·세트·횟수를 전부 손으로 입력해야 한다"* (2025-11-16)
   - *"다른 앱들은 어떤 운동을 어떻게 하는지 알려줬는데 이 앱엔 그게 없다"* (2024-04-24)
2. **비슷한 앱은 있지만 방향이 반대다.** Gymdex(평가 2,274건) 같은 「링크 스크랩」 앱들은 **영상 → 운동 루틴 생성** 방향이고, 전부 **영상을 글자로 바꿔 버린다.**
   우리는 **운동 → 영상 붙이기** 방향이고, **영상을 그대로 남긴다.** 이걸 하는 앱은 조사 범위 안에 없었다.
3. **식단은 Noom이 신호등 방식을 쓰지만**, 운동 기록과 한 앱에 합친 곳은 없다.

**누구를 위한 것인가**
- 1차: **우경님 본인** (내가 4주를 못 쓰면 남도 안 쓴다)
- 2차: 링크를 받아 써볼 지인 — **웹앱이라 링크만 주면 끝**

---

---

## 🧭 설계 원칙 — 이 앱의 헌법 (2026-08-10 우경님 지시)

> ## 🔴 "복잡한 건 안 쓰게 됨."
> — 2026-08-10 우경님. **이 앱이 실패하는 유일한 방법이다.**
>
> **"사용법은 무조건 쉽게. 기록 뎁스는 단순하게. 선택사항도 최대한 알약버튼으로."**

기능을 추가할 때마다 **이 6가지를 먼저 통과해야 한다.** 어기면 그 기능은 넣지 않는다.

| # | 규칙 | 구체적인 선 |
|---|---|---|
| 1 | **2번 안에 끝난다** | 앱을 열고 **탭 2번 안에** 기록·촬영 화면이 나와야 한다. 3번째 탭이 필요하면 설계가 틀린 것 |
| 2 | **입력창은 바로 뜬다** | 「+」를 누르면 **입력 화면이 곧바로** 나온다. "무엇을 추가할까요?" 같은 중간 화면을 넣지 않는다<br>*(한국 다이어트 앱 후기의 실제 불만: "+버튼 눌렀는데 뎁스가 하나 더 있어 또 눌러야 한다")* |
| 3 | **빈칸을 미리 채워둔다** | 세트 추가 → **직전 무게·횟수가 이미 들어가 있다.** 그대로면 ✓만 누르면 끝<br>식단·눈바디 → **오늘 날짜가 자동.** 날짜 고르는 화면 없음 |
| 4 | **설정을 묻지 않는다** | 처음 켤 때 설문·목표설정·프로필 입력 없음. **열자마자 바로 쓸 수 있다.** 필요한 건 나중에 물어본다 |
| 5 | **글자 대신 그림** | 버튼은 아이콘 + 큰 글씨. 설명문을 읽어야 알 수 있는 화면은 실패 |
| 6 | **고르는 건 전부 알약버튼** | ⬇️ 아래 참조 |

### 💊 알약버튼 규칙 (규칙 6 상세)

**드롭다운 · 스크롤 휠 · 자유 입력창을 쓰지 않는다.** 고를 수 있는 건 전부 **톡 누르는 알약**으로.

> ⚠️ **2026-08-10 수정** — *"모든 화면에 알약 넣는 건 오반 거 같아. 강조만 알약으로."* (우경님)
> **알약을 사방에 쓰면 그것대로 시끄럽다.** 알약은 **딱 두 곳**에만 쓴다.

| 알약을 쓰는 곳 | 이렇게 |
|---|---|
| ① **지금 고른 것** | 고른 것만 노란 알약. **나머지 선택지는 색 없는 그냥 글자**<br>`**가슴**` 등 어깨 하체 엉덩이 코어 |
| ② **이 화면의 주요 버튼** | 화면당 **하나만**. 예: `이미지로 저장`, `＋ 세트 추가` |

| 알약을 안 쓰는 곳 | 대신 |
|---|---|
| **무게 입력** | `−` `60kg` `+` ← 동그란 스테퍼. 한 번에 2.5kg |
| **횟수 입력** | `−` `10` `+` ← 같은 스테퍼. 세트 줄 안에 나란히 |
| 눈바디 방향 | 사진 칸 세 개가 나란히 있고 누르면 카메라 (버튼 자체가 필요 없음) |
| 비교 기간 | 고른 것만 알약, `2주` `1주` `처음부터` 는 글자 |
| 판정 배지 | 알약 모양이지만 **버튼이 아니라 결과 표시** — 굿·낫뱃·배드 |

**공통 조건**
- 손가락으로 편하게 눌리는 크기 (최소 44px 높이)
- **한 줄에 6개를 넘지 않는다.** 넘으면 그건 선택지가 너무 많다는 뜻 → 줄인다
- **드롭다운 · 스크롤 휠은 쓰지 않는다**
- 키보드는 **정말 숫자를 직접 쳐야 할 때만** 올라온다

**화면 깊이 지도 (이 이상 깊어지지 않는다)**
```
홈(운동 목록)  ─탭1→  운동 상세 [영상 + 기록]        ← 기록까지 2번
하단탭 [식단]  ─탭1→  카메라                          ← 촬영까지 2번
하단탭 [눈바디] ─탭1→  앞/옆/뒤 칸 → 카메라            ← 촬영까지 2번
하단탭 [기록]  ─탭1→  달력에서 날짜 → 그날 전부        ← 확인까지 2번
```

---

## #2 유저 이용 흐름 (How) — 5단계

> ### 🔴 2026-08-10 구조 변경 — 「운동 하나씩」에서 「루틴 통째로」로
> 우경님: *"차라리 루틴을 만드는 걸로 갈까"*
>
> **근거 — 우경님이 저장한 영상 5개 중 4개가 「루틴」 영상이었다.**
> *부위별 힙운동 **리스트*** / *초보자 힙업 **루틴 딱 5개**로 끝내요* / *케이블로 끝내는 등운동 **루틴*** / *윗쪽 엉덩이 **4가지 루틴***
> → 우경님은 운동을 **하나씩 찾는 게 아니라 묶음으로** 본다. 첫 설계가 실제 흐름과 어긋나 있었다.
>
> | | |
> |---|---|
> | **전** | 홈 → 부위 → 운동 하나 → 기록 → 나가기 → 다시 운동 하나… **4번 반복** |
> | **후** | 홈 → **「힙데이」 한 번** → 운동 4개가 한 화면에 → 위에서 아래로 쭉 |
>
> **헬스장에서 들락날락이 없어진다.** 폰 들고 있는 시간이 줄어든다.

```
[1] 앱 링크를 연다 (폰 브라우저 / 홈화면 아이콘)
        ↓
[2] 오늘 할 루틴을 누른다         예:「힙데이」
     └ 운동 4개가 한 화면에 세로로 뜬다
     └ 지금 하는 것만 펼쳐지고 나머지는 접혀 있다 (0/3, 3/3 진행 표시)
        ↓
[3] 위에서 아래로 내려가며 기록     40kg × 12 → ✓ / + 세트
     └ 각 운동에 내가 고른 영상이 한 줄로 접혀 있다 (누르면 3:20~4:05만 재생)
     └ 「루틴 끝내기 🌰」 → 도토리 하나 쌓임
        ↓
[4] 밥 먹을 때 사진을 찍는다
     └ 탄수 낫뱃 / 단백질 굿 / 지방 굿  + 한 줄 코멘트
     └ 밤에 하루 요약 한 줄: "오늘 3끼 중 2끼 괜찮았어요"
        ↓
[5] 눈바디를 앞·옆·뒤로 찍는다 (하루 1번)
     └ 앞 사람 실루엣 가이드가 흐리게 겹쳐 보여서 매번 같은 자세·거리로 찍힌다
     └ 한 달 뒤 「비교」를 누르면 첫날과 오늘이 나란히 뜬다
```

---

## #3 핵심 기능 (What) — 구체적으로

### A-0. 루틴 (앱의 중심) ⭐

- **홈 = 루틴 목록.** 부위 목록이 아니다.
  - 맨 위: **도토리 저금통** 🌰 — 지금까지 완료한 루틴 개수
  - **「오늘 할 루틴」** 하나가 크게 (주황). 그 아래 **「다른 루틴」** 들
  - 맨 아래 **「스트레칭」** 한 줄 (루틴이 아니라 보관함으로 들어감)
  - 각 줄에 `운동 4개 · 지난번 8/8 · 42분`

- **루틴 화면** — 운동들이 세로로 한 화면에
  ```
  ✓  힙 쓰러스트          3/3   ›     ← 끝난 것 (초록 표시, 접힘)
  2  사이드 킥백          1/3   ⌄     ← 지금 하는 것 (펼쳐짐)
       ▶ 핏주부·윗쪽 엉덩이 4가지 ⏱3:20→4:05
       1  − 15 +  회   ✓
       2  − 15 +  회   ✓
       ＋ 세트 추가
  3  클램쉘              0/3   ›
  4  케틀벨 데드리프트     0/3   ›
  ─────────────────────────
        [ 루틴 끝내기 🌰 ]
  ```
  - **한 번에 하나만 펼쳐진다** (아코디언). 안 그러면 화면이 난장판이 된다
  - 각 운동에 **내가 고른 영상**이 한 줄로 접혀 있다
  - **「루틴 끝내기」** → 도토리 하나가 저금통에 쌓인다

- **루틴 만들기** — 이름 정하고 운동을 골라 담는다. 순서는 끌어서 바꾼다
- 루틴은 **여러 개** 만들 수 있다 (힙데이 / 등·어깨 / 하체 전체 …)
- ⚠️ **스트레칭은 루틴으로 묶지 않는다** — *"여러 개 저장해놓고 필요한 거 쓰는 걸로"* (우경님)

### A. 운동 목록

- **기본 운동 목록을 앱이 제공한다.** 부위 분류 **7개**:
  > **엉덩이 · 하체 · 코어 · 등 · 어깨 · 가슴 · 스트레칭**
  - ⚠️ **「팔」을 빼고 「엉덩이」를 넣었다** (2026-08-10 우경님 지시). 보통 앱들은 엉덩이를 하체에 뭉뚱그리는데, 실제로 따로 찾는 사람이 훨씬 많다. 팔 운동은 「가슴·등」 안에 넣는다
  - ⚠️ **「스트레칭」 추가** (2026-08-10). 근력 부위가 아니라 성격이 달라서 맨 끝에 둔다
  - **순서를 실제로 많이 하는 순으로** 바꿨다 — 엉덩이가 맨 앞 (링크 샘플 5개 중 3개가 힙 운동)
  - ⚠️ **홈트가 아니라 헬스장이다** (2026-08-10 우경님 정정). 기본 운동 목록은 **기구 중심**으로 짠다 — 힙 쓰러스트 · 케이블 · 데드리프트 · 스쿼트 · 케틀벨 등

### C-0. 기록 방식은 운동마다 3가지 중 하나

> 2026-08-10 추가. 계기: 링크 샘플에 **골반교정 스트레칭**이 있었는데, 여기엔 **무게가 없다.**
> 「아침·점심·저녁」이 간헐적 단식에 안 맞았던 것과 **똑같은 문제**다.

| 방식 | 어떤 운동 | 기록 화면 |
|---|---|---|
| **무게 × 횟수** | 힙 쓰러스트, 스쿼트, 케이블 (기구) | `− 40kg +` `− 12 +` |
| **횟수만** | 사이드 킥백, 클램쉘 (맨몸) | `− 15 +` |
| **시간** | 골반교정, 플랭크, 스트레칭 | `− 30초 +` |

- 운동을 만들 때 **한 번만 정하면** 되고, 그 뒤로는 다시 안 물어본다
- **기본 운동 목록에는 미리 정해져 있다** (사용자가 고를 일 없음 — 설계 원칙 4)
- 안 쓰는 칸을 화면에 띄우지 않는다. 스트레칭 화면에 빈 무게 칸이 없다
- 목록 맨 위에 **「+ 내 운동 추가」** 버튼. 누르면 → 운동 이름 입력 + 부위 선택 → **저장** → 목록 맨 위에 추가된다.
- 운동 카드를 누르면 → **운동 상세 화면**으로 이동.
- 운동 카드에는 **가이드 영상이 붙어 있으면 작은 ▶ 표시**가 뜬다. (안 붙은 운동을 한눈에 구분)

### B. 가이드 영상 붙이기 ⭐ 핵심 차별점

> ### 🔴 2026-08-10 중요 수정 — 영상은 **접혀 있다**
> 우경님 원문:
> > *"헬스장에서 써"*
> > *"기구는 대부분 사용법 알아서 일부러 보진 않는데 저장해놓고 보고싶어"*
>
> **영상은 「매번 보는 것」이 아니라 「저장해두고 가끔 꺼내보는 것」이다.**
> 그런데 처음 설계는 운동 상세를 열면 **영상이 화면 절반**을 차지했다. 매번 안 보는 게 자리를 다 먹고, 그만큼 기록까지 손이 멀어진다.
>
> → **영상은 한 줄로 접어둔다. 누를 때만 펼친다.**
> ```
> ▶  핏주부 · 윗쪽 엉덩이 황금동작 4가지        ⌄
>     ⏱ 3:20 → 4:05
> ```
> - 열면 **기록 화면이 바로** 보인다 (설계 원칙 1 — 2번 안에 끝난다)
> - 저장 기능은 그대로. 붙여두는 건 여전히 쉽다
> - 📌 타깃 문장도 미세 조정: *"운동을 아예 모르는 사람"* 이 아니라
>   **"기구는 쓸 줄 알지만, 좋은 루틴 영상을 저장해두고 참고하는 사람"**

### 🔀 근력운동과 스트레칭은 영상을 대하는 방식이 정반대다 (2026-08-10)

우경님 원문:
> *"기구는 대부분 사용법 알아서 일부러 보진 않는데 저장해놓고 보고싶어"*
> *"스트레칭은 여러개 저장해놓고 필요한거 쓰는걸로"*

| | 영상을 | 화면 |
|---|---|---|
| **근력운동** (엉덩이·하체·코어·등·어깨·가슴) | 사용법을 아니까 **거의 안 본다** | 영상은 **한 줄로 접힘**. 기록이 주인공 |
| **스트레칭** | 저장해두고 **골라서 본다** | **썸네일 격자 목록**. 영상이 주인공 |

**스트레칭 화면 (보관함)**
- 부위에서 「스트레칭」을 고르면 → **저장해둔 영상들이 썸네일 2열 격자**로 뜬다
- 각 카드: 썸네일 · 길이(0:30) · 이름 · 채널 · 마지막으로 한 날
- 카드를 누르면 → 재생 + **시간 기록**(30초 등)
- 맨 끝에 **`＋ 영상 추가`** 칸 (링크 붙여넣기)
- **고정된 「매일 세트」를 만들지 않는다.** 그날 필요한 걸 그때 고른다

---

- 운동 상세 화면 맨 위에 **「가이드 영상」 한 줄** (접힌 상태가 기본).
  - 비어 있으면: **「+ 영상 붙이기」** 한 줄
  - 붙어 있으면: **썸네일 + 제목 + 구간** 한 줄. 누르면 펼쳐져 재생, 옆 `⋯` 에 「영상 교체 / 구간 다시」

- **「+ 영상 붙이기」를 누르면** → 링크 붙여넣는 칸이 뜬다. **유튜브 / 틱톡 / 인스타 링크 모두 받는다.**

- **유튜브 링크인 경우** ✅
  1. 영상이 화면에 뜬다.
  2. 아래에 **「⏱ 시작 지점 찍기」 / 「⏱ 끝 지점 찍기」** 두 버튼.
  3. 영상을 보다가 원하는 순간에 누르면 → `시작 3:20` `끝 4:05` 로 저장된다.
  4. **저장** 후부터, 이 운동을 열면 **그 구간부터 재생되고 끝 지점에서 자동으로 멈춘다.**
  5. 구간을 안 정하면 처음부터 전체 재생.

- **틱톡 / 인스타 링크인 경우** ⚠️
  1. 썸네일과 제목만 저장된다.
  2. 구간 찍기 버튼은 **보이지 않고**, 대신 회색 안내가 뜬다:
     *"틱톡·인스타는 앱 안에서 구간 재생이 안 돼요. 눌러서 해당 앱에서 봐주세요."*
  3. 썸네일을 누르면 → 틱톡/인스타 앱(또는 웹)으로 이동한다.

- **영상은 한 번 붙여두면 계속 쓴다.** 「영상 교체」를 누를 때만 바뀐다.
- ⚠️ **영상을 내려받거나 우리 서버에 저장하지 않는다.** 링크로만 참조한다. (저작권)

### C. 운동 기록

- 운동 상세 화면 아래쪽에 **「기록 시작」** 버튼.
- 누르면 **세트 줄이 하나 생긴다.** 알약버튼으로만 조작한다:

  ```
  1세트   [ − ] 60kg [ + ]     [8] [10] [12] [15] [직접]     [ ✓ ]
  ```
  - 무게: `−` `+` 는 **2.5kg씩** 움직인다. 숫자를 직접 누르면 그때만 키보드가 올라온다
  - 횟수: 자주 쓰는 값이 알약으로 있고, 고르면 색이 찬다
- **「+ 세트」** 버튼으로 줄을 추가한다. → **직전 세트의 무게·횟수가 이미 채워져 있다. 그대로면 `✓` 하나만 누르면 끝.**
  *(Hevy 혹평 중 "새 세트가 이전 무게를 안 따라온다"가 있었음 — 그 실수를 피한다)*
- `✓` 를 누르면 그 세트가 **완료 표시**된다.
- **「운동 끝내기」** → 오늘 날짜로 저장되고 운동 목록으로 돌아간다.
- 다음에 그 운동을 열면 영상 아래에 **한 줄**이 보인다: `지난번 · 8/8 · 60kg × 10회 × 3세트`

### D. 식단 사진 판정 ⭐ 핵심 차별점

- **식단** 탭 → 큰 **카메라 버튼**. 사진 찍기 또는 앨범에서 고르기.

- 🕐 **「아침 · 점심 · 저녁」을 쓰지 않는다.** (2026-08-10 우경님 지적)
  > *"하루 4끼 먹거나 간헐적 단식하는 사람 있으니까"*
  - 대신 **사진을 찍은 시각이 자동으로** 들어간다. `12:30`
  - 그 옆에 앱이 **자동으로 세어서** 표시한다. `오늘 두 번째 끼니`
  - **사용자는 아무것도 고르지 않는다.** (설계 원칙 4 — 설정을 묻지 않는다)
  - 2끼든 4끼든 6끼든 그대로 담긴다. 빈 「아침」 칸이 생기지 않는다
  - 📌 시간이 남아있으므로 나중에 *"오늘 먹은 시간 10시간"* 같은 걸 붙일 수 있다 → **지금은 만들지 않음(보류)**

- 사진을 넣으면 AI가 판정하고 **결과 카드**를 보여준다:

  ```
  12:30           오늘 두 번째 끼니

  🍚 탄수화물   낫뱃
  🍗 단백질     굿
  🥑 지방       굿

  "밥이 좀 많긴 한데 단백질이 잘 들어갔어요."
  ```

- **정크푸드가 보이면** 카드 아래에 한 줄 더: `⚠️ 정크푸드 위험도 — 보통`
  (위험도는 **약간 / 보통 / 많음** 3단계)
- ⚠️ **말투 규칙**: 판정 결과는 굿/낫뱃/배드로 쓰되, **코멘트는 나무라지 않는다.**
  *"배드입니다" 대신 "이번 끼는 지방이 좀 셌어요. 다음 끼에 채소 한 번!"*
  (영양사 지적 — 음식에 '나쁨' 딱지는 죄책감을 유발함)
- **밤에 하루 요약 한 줄**: `오늘 3끼 중 2끼 괜찮았어요`
- **칼로리·그램 숫자는 절대 보여주지 않는다.**

### E. 눈바디 기록 ⭐

- 화면 아래 탭에 **[눈바디]** 추가. → **[운동] [식단] [눈바디]**

- **찍기** — 하루 1번, **앞 / 옆 / 뒤** 세 장.
  - 세 칸이 나란히 보이고, 안 찍은 칸은 비어 있다. 칸을 누르면 카메라가 열린다.
  - **찍을 때 화면에 지난번 사진이 아주 흐리게 겹쳐 보인다.** 그 실루엣에 몸을 맞추면 **매번 같은 자세·같은 거리**로 찍힌다.
    *(눈바디 비교가 실패하는 가장 큰 이유가 "각도가 달라서"다. 이 한 가지로 해결된다)*
  - 세 장을 다 안 찍어도 된다. 앞만 찍은 날도 그대로 저장된다.
  - 다시 찍기 = 그날 사진을 덮어쓴다.

- **비교** — 「비교」 버튼을 누르면 **두 날짜를 고른다.** (기본값: `첫날` ↔ `오늘`)
  - 결과는 **콜라주 한 장**: 위아래 두 줄, 왼쪽부터 앞·옆·뒤
    ```
    [ 7/10 앞 ][ 7/10 옆 ][ 7/10 뒤 ]
    [ 8/10 앞 ][ 8/10 옆 ][ 8/10 뒤 ]
    ```
  - 콜라주 아래에 날짜와 **며칠 차이인지**가 적힌다: `31일 차이`
  - **「이미지로 저장」** 버튼 → 콜라주 한 장이 폰 앨범에 저장된다. (남에게 보여주고 싶을 때만 본인이 직접)
  - 한쪽 날짜에 그 방향 사진이 없으면 그 칸은 `사진 없음`으로 비워 둔다.

- **달력 보기** — 찍은 날이 점으로 표시된다. 날짜를 누르면 그날 세 장이 뜬다.

- 🔒 **눈바디 사진은 절대 밖으로 나가지 않는다.**
  - AI에 보내지 않는다. 서버에 올리지 않는다. **폰 안에만 저장된다.**
  - (밥 사진은 판정을 위해 AI로 보내지만, 눈바디는 판정할 게 없으므로 보낼 이유가 없다)
- 📦 **저장할 때 사진 크기를 자동으로 줄인다.** (긴 변 1080px, 장당 200KB 내외)
  줄이지 않으면 매일 3장 × 한 달 = 90장에서 폰 저장 공간이 찬다.

### F. 기록 탭 — 달력 하나로 전부 보기

> 2026-08-10 추가. 우경님 지적: *"저장된 건 어디서 봐?"* — 지난 기록을 볼 곳이 없었다.

- 화면 아래 탭이 **4개**가 된다: **[운동] [식단] [눈바디] [기록]**
- **기록** 탭 = **달력 하나.**
  - 날짜 밑에 **점 세 색**으로 그날 뭘 했는지 표시: 🟡운동 / 🟢식단 / 🔵눈바디
  - 날짜를 누르면 **그날 한 걸 한꺼번에** 보여준다:
    ```
    8월 3일 (일)
    🏋️  벤치프레스 60kg × 10 × 3 / 인클라인 덤벨 14kg × 12 × 3
    🍚  08:20 굿 · 12:30 낫뱃 · 19:10 굿
    📸  눈바디 3장 (앞·옆·뒤)
    ```
  - `‹` `›` 로 달을 넘긴다.
- **왜 탭 3개 안에 각각 넣지 않았나** — 그러면 *"그날 뭐 했지"* 를 보려고 **세 군데를 따로 뒤져야** 한다. 한 곳에 모으면 한 번에 답이 나온다. 여전히 **탭 2번 안**이다.

### G. 처음 시작할 때 — 내 데이터 가져오기

**앱을 만들자마자 빈 껍데기가 아니라 「내 앱」이 되게 한다.**

- 설정 화면에 **「Hevy 기록 가져오기」** 버튼. (안 해도 앱은 그냥 돌아간다 — 설계 원칙 4)
- Hevy 앱 설정에서 **데이터 내보내기**를 요청하면 **메일로 CSV 파일**이 온다. **무료 기능이다.** (2026-08-10 확인)
- 그 파일을 앱에 넣으면:
  1. **내가 실제로 하는 운동들이 목록에 채워진다.** → 기본 목록을 뒤질 필요가 없다
  2. **지난 기록이 옮겨온다.** → 첫날부터 `지난번 · 60kg × 10회 × 3세트` 가 보인다
- 가져오기는 **한 번만** 하면 된다. 이후는 이 앱에서 기록한다.

### H. 저장과 백업

- 기록은 **폰 브라우저 안에 저장**된다. 로그인 없이도 각자 자기 것만 보인다.
- 설정 화면에 **「내보내기」 / 「가져오기」** 버튼.
  - 내보내기 → 파일 하나로 저장 (폰 바꿀 때 대비). **눈바디 사진도 함께 들어간다.**
  - 가져오기 → 그 파일을 넣으면 복원
  *(Hevy 혹평 2위가 "데이터 유실" — 로그아웃 한 번에 운동 200개가 사라졌다는 후기가 있었다)*
- ⚠️ 사진이 쌓이면 브라우저 저장 공간이 언젠가 찬다. 설정에서 **지금 몇 장 / 얼마나 쓰는지**를 보여주고,
  한계에 가까워지면 *"내보내기로 백업하고 오래된 사진을 정리하세요"* 라고 알린다.

---

## #4 Out of Scope — 이번엔 절대 안 만든다

| 안 만드는 것 | 왜 |
|---|---|
| **로그인 · 회원가입** | 폰 안에 저장하면 충분. 배포와 로그인은 별개다. 나중에 붙일 수 있게 설계만 열어둔다 |
| **소셜 · 피드 · 공유** | Hevy 혹평: *"소셜 끄기 설정을 해도 Discover 피드가 남는다"* |
| **애플워치 연동** | Hevy 혹평 80건 중 **20건 넘게** 이것. 최대 지뢰밭 |
| **칼로리 · 그램 숫자** | 사진으로는 양을 알 수 없어 어차피 틀린다. 틀린 숫자는 신뢰를 무너뜨린다 |
| **결제 · 구독** | 내가 쓸 앱 |
| **AI 자동 구간 탐색** | "영상에서 스쿼트 나오는 데 찾아줘"는 자꾸 틀리고 느리고 비싸다. 내가 직접 두 번 찍는 게 정확하다 |
| **영상 다운로드 · 재업로드** | 저작권. 링크로만 참조한다 |
| **운동 루틴 자동 생성 · AI 코치** | Gymdex·Fitbod가 이미 함. 우리 문제가 아니다 |
| **네이티브 앱(앱스토어 출시)** | 웹앱이면 링크로 끝난다 |
| **AI 체형 점수 · 눈바디 평가** | PeakBFF가 하는 것. 점수는 위축시킨다. **우리는 나란히 보여주기만 한다** |
| **눈바디 사진을 AI·서버로 보내기** | 민감한 사진. 판정할 게 없으니 보낼 이유도 없다. **폰 안에만** |
| **첫 실행 설문 · 목표 몸무게 설정** | 설계 원칙 4번 위반. 열자마자 바로 쓸 수 있어야 한다 |
| **타임랩스 영상 만들기** | 눈바디 콜라주로 충분. 나중에 아쉬우면 그때 |

---

## #5 성공 지표 (KPI)

**주 지표 — 이거 하나로 성공/실패를 판정한다**
> ✅ **4주 동안, 주 3회 이상 루틴을 완료했다 — 즉 🌰 도토리 12개 이상.**
> 12회 미만이면 **실패로 인정하고** 왜 안 열게 됐는지부터 찾는다.

**보조 지표**
- 가이드 영상을 붙여둔 운동이 **5개 이상**  ← 핵심 기능이 실제로 쓰였는가
- 식단 사진 판정을 **주 5회 이상**  ← 귀찮지 않았는가
- 눈바디를 **주 3일 이상** 찍었고, 한 달 뒤 **비교 콜라주를 실제로 봤다**  ← 이걸 봤다면 앱이 제 역할을 한 것
- ⏱ **세트 하나 기록에 5초 이내**  ← 설계 원칙이 지켜졌는가를 재는 자

**"이 기능 있으면 성공, 없으면 실패" 판정선**
- 🟢 **있어야 성공**: 운동에 내가 고른 영상 붙이기 + 유튜브 구간 재생 + 사진 3단계 식단 판정 + 눈바디 비교 콜라주
- 🔴 **있으면 오히려 실패**: 칼로리 숫자, 소셜 피드, 로그인 강제, **드롭다운·설문 화면**

---

---

## 🔗 링크 처리 규칙 (실제 샘플로 확인 — 2026-08-10)

### 샘플 ① 유튜브 · 재생목록 안의 영상

```
https://www.youtube.com/watch?v=6BTcz1K1YuQ&list=PLY00hOMX8M7BIiuo5vWkLXwL7Do2U3WVV&index=6
```
> 「부위별 힙운동 추천 리스트📋 (1달 찐후기, 힙 하부, 위+옆)」 — 제이제이살롱드핏

**🚨 반드시 지킬 것 — `list` 와 `index` 를 떼어내고 `v` 만 쓴다.**

| 파라미터 | 값 | 처리 |
|---|---|---|
| `v` | `6BTcz1K1YuQ` | ✅ **이것만 쓴다** |
| `list` | `PLY00hOMX8M7...` | ❌ **버린다** |
| `index` | `6` | ❌ **버린다** |

**왜** — `list`를 남겨두면 **재생목록 전체가 재생된다.** 4:05에서 멈춰야 하는데 다음 영상으로 계속 넘어간다. 이걸 놓치면 핵심 기능이 통째로 망가진다.

### 제목·썸네일 가져오기 — API 키 필요 없음

```
제목·채널명:  https://www.youtube.com/oembed?url=https://www.youtube.com/watch?v=<영상ID>&format=json
썸네일:       https://i.ytimg.com/vi/<영상ID>/hqdefault.jpg
```
> 2026-08-10 실제로 작동 확인. 유튜브 데이터 API 키를 발급받을 필요가 없다.

### 💡 한 영상 → 여러 운동 (이 샘플이 증명한 것)

이 영상은 **여러 운동을 쭉 보여주는 리스트 영상**이다 (힙 하부 / 힙 위 / 힙 옆).
→ **같은 영상을 여러 운동에 붙이고 구간만 다르게 하면 된다.**

```
힙 쓰러스트   → 6BTcz1K1YuQ · 3:20 ~ 4:05
사이드 킥백   → 6BTcz1K1YuQ · 5:10 ~ 6:00
```

- 우리 설계(운동 → 영상 붙이기)는 이걸 **그냥 된다.** 별도 기능이 필요 없다
- **Gymdex 같은 「영상 → 글자」 앱은 이걸 못 한다.** 동작이 어떻게 생겼는지가 사라지기 때문
- ⚠️ 저장할 때 **영상ID + 시작초 + 끝초** 세 개만 저장한다. 영상 자체는 저장하지 않는다

### 샘플 ② 유튜브 · 시작 시각이 붙은 링크

```
https://www.youtube.com/watch?v=nQlbDogfCpE&t=2s
```
> 「몸을 다시 세우는 기적의 골반교정! (생리통, 다리꼬는 습관, 얼굴비대칭👍🏻)」 — 소미핏

**`t=2s` 는 버리지 말고 「시작 지점」 기본값으로 쓴다.** ⭐

| 파라미터 | 값 | 처리 |
|---|---|---|
| `v` | `nQlbDogfCpE` | ✅ 영상 ID |
| `t` | `2s` | ✅ **시작 지점을 2초로 미리 채워둔다** |

**왜** — 사용자가 *"여기부터 보면 돼"* 라고 이미 정해서 저장한 링크다.
그 정보를 버리고 처음부터 다시 찍게 하면 **한 일을 두 번 시키는 것**이다.
→ **설계 원칙 3(빈칸을 미리 채운다)** 에 정확히 해당한다. 끝 지점만 찍으면 된다.

- `t` 는 `2s` / `90` / `1m30s` 등 여러 모양으로 온다. 전부 **초로 바꿔서** 저장한다

### 지금까지 확인된 유튜브 주소 모양 3종

| 모양 | 예 | 처리 |
|---|---|---|
| 기본 | `watch?v=ID` | `v` 만 |
| 재생목록 안 | `watch?v=ID&list=…&index=6` | `list`·`index` **버림** |
| 시작시각 있음 | `watch?v=ID&t=2s` | `t` → **시작 지점 기본값** |
| (예상) 짧은 주소 | `youtu.be/ID` | 뒤쪽이 영상 ID |
| (예상) Shorts | `youtube.com/shorts/ID` | 뒤쪽이 영상 ID |

### 샘플 ③④⑤ 유튜브 Shorts

```
https://www.youtube.com/shorts/lw2M9PvXDho   초보자 힙업 루틴 딱 5개로 끝내요 — 핏주부
https://www.youtube.com/shorts/SI09b1ewKkc   케이블로 끝내는 등운동 루틴 — 썽이핏
https://www.youtube.com/shorts/e-MTQ53GMrs   윗쪽 엉덩이 키우는 황금동작 4가지 — 핏주부
```

- `youtube.com/shorts/<ID>` → **`watch?v=<ID>` 로 바꿔서** 처리하면 된다
- **oEmbed가 Shorts 주소 그대로도 작동한다** (2026-08-10 확인)
- ⚠️ Shorts는 보통 60초 이하 → **구간 지정 버튼을 띄우지 않는다.** 통으로 본다
  (안 그러면 15초짜리에서 구간을 찍으라는 꼴 — 설계 원칙 위반)

### 📊 샘플 5개에서 읽은 것

| 발견 | 근거 | 설계 반영 |
|---|---|---|
| **힙·엉덩이가 압도적** | 5개 중 3개 | 부위 순서 맨 앞으로 |
| **대부분 「루틴/리스트」 영상** | "4가지 루틴" "딱 5개로" "부위별 리스트" | 한 영상 → 여러 운동에 구간만 다르게 |
| **Shorts 비중 높음** | 5개 중 3개 | Shorts는 구간 지정 안 함 |
| **헬스장 기구 운동** | 케이블·케틀벨·데드리프트 | ⚠️ 홈트 아님 (우경님 정정). 기본 목록은 기구 중심 |
| **스트레칭도 있음** | 골반교정 | 기록 방식 「시간」 필요 |

### 아직 못 받은 샘플
- [ ] **인스타 릴스** 1개
- [ ] **틱톡** 1개
> 없으면 유튜브만으로 만들어도 된다. 릴스·틱톡은 어차피 구간 재생이 안 되므로 나중에 붙여도 무방.

---

## 📌 기술 방향 (참고 — 3단계에서 확정)

| 항목 | 안 | 근거 |
|---|---|---|
| 형태 | **웹앱** (폰 홈화면에 추가해서 앱처럼) | 링크로 남에게 바로 공유 가능 |
| 스택 | Next.js + Vercel 배포 | 날씨앱에서 이미 해본 방식 |
| 식단 판정 | Gemini (사진 입력) | 날씨앱 챗봇에서 쓰던 것 재사용 |
| 유튜브 구간 재생 | 유튜브 임베드의 시작·끝 지정 기능 | 공식 지원됨 |
| 저장 | 폰 브라우저 안(로컬) + 내보내기 파일 | 로그인 없이 각자 분리 |

---

---

## 📥 우경님이 준비해주실 재료

| 언제 | 무엇 | 왜 |
|---|---|---|
| **지금 (설계용)** | **유튜브 링크 3~5개만** — 실제로 따라 하는 운동 영상으로 | 링크 생김새마다 처리가 달라요 (일반 영상 / Shorts / 재생목록 / 시간이 붙은 링크). **우경님이 실제로 쓰는 형태**로 만들어야 구간 재생이 제대로 됩니다 |
| **지금 (설계용)** | 틱톡·인스타 링크 **1~2개** | 그 앱으로 넘어가는 게 잘 되는지 확인용 |
| **만들기 직전** | **Hevy CSV 파일** (Hevy 설정 → 데이터 내보내기 → 메일로 받기, 무료) | 내 운동 목록과 지난 기록이 첫날부터 들어있게 |
| **만든 다음** | 저장해둔 유튜브 링크 **전체** | 운동마다 하나씩 붙이기. 앱이 다 만들어진 뒤 한 번에 |

> 💡 **지금은 3~5개면 충분합니다.** 전체 리스트는 앱이 다 만들어진 뒤에 넣는 게 순서예요.

---

## ✅ 다음 단계

- [ ] 우경님이 이 PRD 읽고 **수정** (기획 규칙: 초안은 AI, 수정은 내가)
- [ ] 앱 이름 정하기
- [ ] **유튜브 링크 3~5개 받기**
- [ ] 3단계 — 스펙 → 계획 → 잘게 쪼개기 → 구현
